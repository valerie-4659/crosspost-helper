feat: editable AI result, modern filter UI, per-network skip, history page

AI Post Panel
- Result fields (title, description, tags) are now editable before queuing
- Queue for Extension and Copy use the user-edited values
- QT Event: new multi-line format with theme + emoji + tagline + let's see
- No perspective: neutral image description instead of first-person voice

Library UI modernisation
- FilterBar: replaced native <select> with appearance-none styled selects
  + chevron icon; fixed undefined→"" binding so current value is always shown
- Toggle filters (incl. skipped / incl. excluded / posted anywhere) are now
  plain pill buttons — no visible checkboxes, consistent with rest of UI
- Sort controls in header replaced with 3 pill buttons (Date | Name | Pick)
- Hide-posted control: styled grouped select + eye-toggle button
- Toolbar & collection-panel selects: same appearance-none treatment

Image exclude
- ImageCard: new per-network Skip button (EyeOff) next to Mark Posted
  → writes status=skipped in post_records for that target only
  → image hidden for that network in Picker + Library; other networks unaffected
- Global Exclude (Archive) button now amber-coloured with clear tooltip
- Skip button turns red when active; clicking again restores to unset

History page
- New "History" entry in sidebar (Clock icon, between Job Queue and Scan)
- Shows all posted images grouped by day with thumbnail + network badge
- Filter chips per network type at the top
